# 龙宝宝养成游戏 - Vibe Coding 启动 Prompt

下面是为你准备的完整 Prompt。**你可以直接复制到 Cursor / Claude / Copilot 中开始 vibe coding**。

我把它设计成两个版本：
- **版本 A**：一次性完整 Prompt（适合让 AI 一口气搭好原型）
- **版本 B**：分阶段 Prompt（适合一步步迭代，更可控）

---

## ? 版本 A：完整启动 Prompt（推荐先用这个）

```markdown
# 项目：龙宝宝工厂养成游戏 - 极简 MVP

## 角色与目标
你是一名经验丰富的游戏开发工程师，精通 Phaser 3 + TypeScript。
我是一名 UE5 资深开发者。
请帮我搭建一个"龙宝宝养成 + 简化版 Factorio"的极简原型。

## 游戏核心概念

这是一个混合类型游戏，结合了：
- Tamagotchi（电子宠物）：玩家养育一只龙宝宝
- Factorio（工厂自动化）：玩家建造工厂自动生产食物喂龙

### 原则 1：逻辑层与渲染层分离

工厂逻辑必须脱离场景渲染独立运行。
即使玩家在龙宝宝场景，工厂依然在后台运行（生产食物）。

core/ ← 纯逻辑，不依赖 Phaser entities/ ← 渲染层，订阅 core 状态

### 原则 2：统一逻辑帧

工厂使用 10Hz 的逻辑帧（每秒 10 次更新）。
渲染帧（60Hz）只负责把当前状态画出来。

### 原则 3：场景共享数据

通过 Phaser Game Registry 在场景间共享：
- factoryWorld（工厂世界状态）
- dragonState（龙宝宝状态）

## 项目结构

## 技术栈
- **引擎**：Phaser 3.80+
- **语言**：TypeScript（严格模式）
- **构建**：Vite
- **代码组织**：清晰的模块化，但不要过度工程化（这是原型）
- **资源**：暂用纯色矩形/圆形作为占位图，不需要美术资源

## 核心游戏循环（最小可行版本）

```
使用流水线工厂制作大量食物 → 投喂龙宝宝 → 龙宝宝反馈 → 产出龙鳞 → 解锁新内容
       ↑                                              ↓
       └──────────────── 用龙鳞建造更多设施 ←───────────┘
```

┌──────────────────────────────────────────────────────────────┐
│                    工厂场景 (FactoryScene)                    │
│                                                              │
│   ┌─[采集器]─┐                                               │
│   │  ?水    │──[传送带]──┐                                  │
│   └─────────┘              ↓                                 │
│                       ┌─[生产机器]─┐                          │
│   ┌─[采集器]─┐          │  混合     │──[传送带]──┐            │
│   │  ?小麦  │──[传送带]→│  烘烤     │            ↓            │
│   └─────────┘          │           │     ┌─[生产机器]─┐       │
│                       └───────────┘      │  组装      │       │
│   ┌─[采集器]─┐                           │  打包      │──┐    │
│   │  ?肉    │────[传送带]──────────────→│           │  │    │
│   └─────────┘                           └───────────┘  │    │
│                                                         │    │
│   ┌─[采集器]─┐                                          │    │
│   │  ?糖    │────[传送带]─[分流器]─→（备用）            │    │
│   └─────────┘                  └────[传送带]────────────┤    │
│                                                         ↓    │
│                                                ┌─[喂食仓]─┐  │
│                                                │ ? 入口  │  │
│                                                └──────────┘  │
└────────────────────────────────────────────────────┬─────────┘
                                                     │
                                            (食物送往龙宝宝)
                                                     │
                                                     ↓
┌──────────────────────────────────────────────────────────────┐
│                    龙宝宝场景 (DragonScene)                   │
│                                                              │
│                          ?                                  │
│                       龙宝宝吃下食物                          │
│                       满意度上升                              │
│                       产出龙鳞                                │
│                                                              │
│                    龙鳞 → 用于建造工厂                        │
└──────────────────────────────────────────────────────────────┘

## MVP 必须包含的功能（按优先级）

### ? 第一优先级：让龙宝宝"活起来"
1. **龙宝宝实体**
   - 屏幕中央显示一只龙宝宝（用一个会变色的圆形+三角形组合代替）
   - 4 种状态对应 4 种颜色：
     - 开心 = 粉色
     - 普通 = 浅蓝
     - 饥饿 = 橙色
     - 不开心 = 暗灰
   - 龙宝宝持续做"呼吸动画"（轻微缩放）
   - 状态切换时有平滑过渡


3. **满意度系统**
   - 满意度 0-100，初始 50
   - 喂食物会上升
   - 影响龙鳞产出速度

### ? 第二优先级：基础喂养循环
4. **食物系统**
   - 3 种基础食物，用不同颜色的方块表示：
     - ? 面包（黄色方块）：饥饿+15，满意+5
     - ? 肉（红色方块）：饥饿+25，满意+10
     - ? 蛋糕（粉色方块）：饥饿+10，满意+20
   - 屏幕底部显示 3 个食物按钮
   - 点击食物按钮 → 食物从按钮飞向龙宝宝 → 龙宝宝吃下 → 状态更新
   - 吃下时有粒子效果和"啊呜"的音效（暂用文字弹出代替音效）

5. **食物库存系统**
   - 每种食物有数量
   - 初始：面包×10、肉×5、蛋糕×3
   - 吃完会显示"食物耗尽"
   - 后续可以通过"工厂"补充

### ? 第三优先级：核心反馈循环
6. **龙鳞产出系统**
   - 满意度 ≥ 60 时，每 5 秒产出 1 片龙鳞
   - 满意度 ≥ 80 时，每 3 秒产出 1 片龙鳞
   - 龙鳞掉落时有视觉效果（金色小方块从龙宝宝身上飘出）
   - 屏幕右上角显示龙鳞总数

7. **简单的"工厂"补充机制**
    使用快捷键或者切换按钮，可以切换到工厂面板，这里能看到工厂所有的生产流程。所有的生产的逻辑都要同时运行不管是不是在渲染。
    工厂使用流水线生产，需要制作传送带、生产机器。假设目前原料中有水、小麦、肉、糖这四种原型食品，需要使用生产机器进行生产（生产机器只做一种就好了）。点击生产机器可以选择配方，配方先使用AI生成简单的配方，基本就是2合一或者3合一，最终可以产出面包、肉、蛋糕就可以。生产需要时间，每个配方都包含原料数量、产物数量、生产时间这三种，生产机器有3个进料口，3个出料口，使用传送带运输原料或者成品进出生产机器。生产机器正在运作的时候，需要持续播放动画（先不做，可以换色代替）。
    传送带需要制作均匀分带&合并装置，还要做地下传送带允许交叉。（MVP阶段先不实现，可以占位）

   - 屏幕左侧三个按钮：制作面包、制作肉、制作蛋糕
   - 消耗龙鳞制作食物：
     - 面包：1 龙鳞 = 5 个面包
     - 肉：3 龙鳞 = 5 个肉
     - 蛋糕：5 龙鳞 = 5 个蛋糕
   - 这就是原始的"工厂"概念，后续会扩展

? 采集器/源头建筑（Source/Spawner）
水源、麦田、牧场、甘蔗田
持续产出对应原料
可以是固定位置的"无限资源点"（MVP 阶段）

? 龙宝宝喂食仓（Dragon Feeder）
工厂的"终点"
食物进入仓后自动消耗，转化为对龙宝宝的喂食

? 网格地图系统（Grid System）
工厂面板是一张网格地图（如 32×32 单元格）
所有建筑占据 1 个或多个网格
鼠标拖拽放置

? 建造模式（Build Mode）
选择建筑类型 → 鼠标变成预览 → 点击放置
消耗龙鳞建造（连接核心循环）
右键拆除（返还部分龙鳞）

注明：MVP阶段，机器和传送带先用固定布局，先不做建造系统

? 物品在传送带上的渲染
即使是占位图，也要看到方块在传送带上滑动

? 逻辑分离原则
工厂逻辑用"逻辑帧"（如每秒 10 次更新）
渲染时才把当前状态画出来
不渲染时只跑数据更新




### ? 第四优先级（可选，时间够再做）
8. **表情反馈**
   - 龙宝宝头上偶尔出现表情气泡：
     - "好饿..." (饥饿度 < 30)
     - "好吃！" (吃完食物)
     - "好开心~" (满意度 > 80)
     - "..." (满意度 < 30)


## 不要做的事（避免过度工程）

? 不要做完整的科技树
? 不要做品质分级系统
? 不要做多种龙
? 不要做复杂的UI（HUD 简洁就行）
? 不要做存档系统
? 不要做音效（先用文字弹出代替）
? 不要写测试代码
? 不要做完整的状态管理库（用简单的 class 即可）
? 不要追求美术质感（占位图就行）

## 项目结构建议

dragon-game/
├── src/
│   ├── main.ts                          # 入口
│   │
│   ├── scenes/                          # Phaser 场景
│   │   ├── BootScene.ts                 # 启动加载
│   │   ├── DragonScene.ts               # 龙宝宝主场景（原 MainScene）
│   │   ├── FactoryScene.ts              # ? 工厂场景
│   │   └── HUDScene.ts                  # ? 全局 HUD（顶部状态栏，跨场景持续显示）
│   │
│   ├── entities/                        # 渲染层实体（视觉表现）
│   │   ├── dragon/
│   │   │   ├── Dragon.ts                # 龙宝宝
│   │   │   └── SpeechBubble.ts          # 表情气泡
│   │   ├── factory/                     # ? 工厂实体
│   │   │   ├── Building.ts              # 建筑基类
│   │   │   ├── Source.ts                # 采集器（水/麦/肉/糖源）
│   │   │   ├── Machine.ts               # 生产机器
│   │   │   ├── Belt.ts                  # 传送带
│   │   │   ├── Splitter.ts              # 分流器/合并器
│   │   │   ├── UndergroundBelt.ts       # 地下传送带
│   │   │   ├── DragonFeeder.ts          # ? 喂食仓（工厂终点）
│   │   │   └── ItemSprite.ts            # 传送带上滑动的物品视觉
│   │
│   ├── core/                            # ? 核心逻辑层（不依赖 Phaser 渲染）
│   │   ├── factory/
│   │   │   ├── FactoryWorld.ts          # 工厂逻辑总控（持续运行）
│   │   │   ├── Grid.ts                  # 网格系统
│   │   │   ├── Cell.ts                  # 单元格
│   │   │   ├── Item.ts                  # 物品数据
│   │   │   ├── BeltNetwork.ts           # 传送带网络（处理物品流动）
│   │   │   ├── MachineLogic.ts          # 机器生产逻辑
│   │   │   └── Recipe.ts                # 配方系统
│   │   └── dragon/
│   │       ├── DragonState.ts           # 龙宝宝核心状态
│   │       └── DragonLogic.ts           # 饥饿、满意度等逻辑
│   │
│   ├── systems/                         # 系统层（连接 core 和 entities）
│   │   ├── HungerSystem.ts              # 饥饿度（先不做）
│   │   ├── HappinessSystem.ts           # 满意度
│   │   ├── ScaleSystem.ts               # 龙鳞产出
│   │   ├── FeedingSystem.ts             # 喂养（连接工厂→龙宝宝）
│   │   ├── BuildSystem.ts               # ? 建造系统
│   │   └── FactoryTickSystem.ts         # ? 工厂逻辑帧驱动
│   │
│   ├── ui/                              # ? UI 组件
│   │   ├── HUD.ts                       # 状态栏
│   │   ├── BuildToolbar.ts              # 建造工具栏
│   │   ├── RecipeSelector.ts            # 配方选择面板
│   │   └── SceneSwitcher.ts             # 场景切换按钮
│   │
│   ├── data/                            # 配置数据
│   │   ├── FoodData.ts                  # 食物定义
│   │   ├── RecipeData.ts                # ? 配方定义
│   │   ├── BuildingData.ts              # ? 建筑定义（成本、属性）
│   │   ├── ItemData.ts                  # ? 物品定义（原料+成品）
│   │   └── DialogueData.ts              # 对白池
│   │
│   ├── events/                          # ? 事件总线
│   │   └── EventBus.ts                  # 全局事件中心
│   │
│   └── utils/                           # ? 工具
│       ├── GameClock.ts                 # 游戏时钟（统一逻辑帧）
│       └── GridHelper.ts                # 网格坐标转换
│
├── index.html
├── package.json
├── tsconfig.json
└── vite.config.ts

## 代码质量要求

- TypeScript 严格模式
- 关键逻辑加简短中文注释（解释"为什么"，不解释"是什么"）
- 用清晰的事件机制连接系统（Phaser 自带的 EventEmitter）
- 数据和逻辑分离（食物配置在 data/ 中，便于后续调整）
- 不要用 any 类型
- 控制台输出关键事件（方便调试）

视觉设计（占位图规范）
所有建筑用纯色矩形 + 文字标签：

采集器：64×64 灰色方块，中间显示物品图标（emoji 或文字）
机器：96×96 蓝色方块，中间显示配方名，运行时颜色变深
传送带：32×16 黑色长条，上面有物品在滑动
物品：16×16 彩色小方块（颜色对应物品类型）
喂食仓：80×80 粉色方块，中间显示 ?
开发步骤（请逐步执行，每步等我确认）
Step 1：项目初始化 + 基础场景
Vite + Phaser + TypeScript 项目骨架
三个场景：DragonScene、FactoryScene、HUDScene
Tab 键切换场景
HUDScene 显示在最上层（顶部状态栏）
提交后我能看到：黑色画面，按 Tab 切换两个场景
Step 2：核心架构 - GameClock 和 EventBus
实现 GameClock（10Hz 逻辑帧驱动器）
实现 EventBus（全局事件总线）
验证：在 DragonScene 时，FactoryScene 的逻辑也在跑（用 console.log 验证）
Step 3：龙宝宝核心
DragonState（core 层，纯数据）
Dragon（entities 层，可视化）
饥饿、满意度系统
龙宝宝在 DragonScene 中显示并会"饿"
Step 4：工厂核心 - 数据层
Item、Recipe 数据定义
Source 逻辑（持续产出物品到内部缓冲区）
Machine 逻辑（按配方消耗输入、产出输出）
Belt 逻辑（物品队列在传送带上移动）
注意：这一步全部是纯逻辑，无渲染
在控制台输出："采集器产出 1 单位水"、"机器开始生产面包"等
Step 5：工厂场景渲染层
固定布局：4 个采集器 + 3 个机器 + 1 个喂食仓 + 连接的传送带
采集器、机器、传送带的 Sprite
物品在传送带上滑动的视觉效果
机器运行时换色
Step 6：连接喂食仓与龙宝宝
物品到达喂食仓 → EventBus 发出 "feed_dragon" 事件
DragonScene 中的 FeedingSystem 监听该事件
龙宝宝数值更新，播放反馈动画
Step 7：龙鳞产出
满意度高时持续产出龙鳞
HUD 显示龙鳞总数（带数字弹跳动画）

我的要求
每完成一步，给我必要的命令、完整代码文件、关键概念解释
用 UE 概念类比解释 Phaser/TS 概念
不要引入额外的库（除了 Phaser 和 TypeScript）
代码要清晰但不过度抽象（这是原型）
关键地方加中文注释解释"为什么"

## 重要说明

- 我是 UE 开发者，请用游戏引擎类比解释概念：
  - Phaser Scene ≈ UE Level
  - GameObject ≈ Actor
  - Phaser Events ≈ UE Delegates
  - Update Loop ≈ Tick

- 如果遇到 JS/TS 生态特有的概念（如 npm、模块系统、Promise），请简短解释清楚

- 当代码涉及 TypeScript 高级特性时，告诉我"这相当于 C++ 的 XXX"

让我们开始吧。请从 Step 1 开始。
```

---

## ? 版本 B：分阶段 Prompt（每步独立，更可控）

如果你想更精细控制，可以分步使用：

### Step 1 Prompt：项目搭建

```markdown
我要做一个龙宝宝养成游戏的 Phaser 3 + TypeScript 项目。
我是 UE5 开发者，但对前端生态不熟。

请帮我从零搭建项目骨架：
1. 用 Vite 创建项目
2. 安装 Phaser 3.80+ 和 TypeScript
3. 配置严格模式
4. 给我一个简单的 main.ts，启动一个 800x600 的黑色画面
5. 列出所有需要执行的命令（npm create, npm install 等）

要求：
- 所有配置文件给完整代码（package.json, tsconfig.json, vite.config.ts, index.html）
- 项目结构按 src/scenes, src/entities, src/systems 组织
- 解释关键步骤（特别是和 Unity/UE 不同的地方）
```

### Step 2 Prompt：龙宝宝实体

```markdown
基于已搭建的 Phaser + TS 项目，我现在要做核心：龙宝宝。

需求：
1. 在屏幕中央显示一只龙宝宝（用圆形+三角形的几何图形组合，不需要美术资源）
2. 龙宝宝有 4 种状态对应 4 种颜色：
   - 开心(满意度>70) = 粉色 #FFB6C1
   - 普通 = 浅蓝 #87CEEB
   - 饥饿(饥饿度<30) = 橙色 #FFA500
   - 难过(满意度<30 || 饥饿度<10) = 暗灰 #696969
3. 龙宝宝持续做"呼吸动画"（每 2 秒一次轻微缩放）
4. 状态切换有 0.5 秒平滑过渡（颜色 tween）

数值系统：
- 饥饿度 0-100，初始 80，每 3 秒 -1
- 满意度 0-100，初始 50

请创建：
- src/entities/Dragon.ts
- src/systems/HungerSystem.ts
- src/systems/HappinessSystem.ts
- 修改 MainScene.ts 把龙宝宝放进去

代码要求：
- 用 EventEmitter 在系统间通信（Dragon 不直接修改数值，由系统驱动）
- 类型严格
- 关键地方加中文注释解释"为什么"
- 在控制台输出状态变化（debug 用）

我希望看到一只在屏幕上会"饿"的龙。
```

### Step 3 Prompt：喂养循环

```markdown
龙宝宝已经能在屏幕上活动了。现在加入喂养机制。

需求：
1. 屏幕底部三个食物按钮（80x80 像素，水平排列）
   - ? 面包（黄色 #FFD700）：饥饿+15，满意+5
   - ? 肉（红色 #DC143C）：饥饿+25，满意+10
   - ? 蛋糕（粉色 #FFB6C1）：饥饿+10，满意+20

2. 食物库存：
   - 初始：面包×10、肉×5、蛋糕×3
   - 按钮上显示数量
   - 数量为 0 时按钮变灰，不可点击

3. 点击食物按钮的交互流程：
   - 食物从按钮位置飞向龙宝宝（200ms 抛物线动画）
   - 飞到龙宝宝身上时：
     - 食物消失
     - 龙宝宝身上爆发粒子效果（同色小圆点）
     - 数值更新
     - 显示文字气泡："好吃！"或"再来一份！"等
   - 库存数量 -1

4. 数据驱动：
   - 食物配置放在 src/data/FoodData.ts
   - 方便我后续添加新食物

请创建/修改：
- src/data/FoodData.ts
- src/entities/Food.ts（如果需要）
- src/systems/FeedingSystem.ts
- 更新 MainScene.ts

让我能"喂"我的龙了！
```

### Step 4 Prompt：龙鳞产出

```markdown
喂养循环已经能跑了。现在加入正反馈：龙鳞系统。

需求：
1. 龙鳞产出规则：
   - 满意度 < 60：不产出
   - 满意度 60-80：每 5 秒产出 1 片
   - 满意度 ≥ 80：每 3 秒产出 1 片

2. 龙鳞掉落动画：
   - 从龙宝宝身上向上飘出（金色 #FFD700 小方块 16x16）
   - 上升+左右摇摆，1.5 秒后消失
   - 同时屏幕右上角的"龙鳞计数"+1，伴随数字弹跳动画

3. 工厂系统（屏幕左侧）：
   - 三个工厂按钮：
     - "造 5 个面包"（消耗 1 龙鳞）
     - "造 5 个肉"（消耗 3 龙鳞）
     - "造 5 个蛋糕"（消耗 5 龙鳞）
   - 龙鳞不够时按钮灰色不可点
   - 点击后：消耗龙鳞 → 对应食物库存 +5 → 文字提示"已制作！"

4. UI 布局：
   - 顶部：龙鳞计数（大字）、饥饿度条、满意度条
   - 左侧：工厂按钮
   - 中间：龙宝宝
   - 底部：食物按钮

请创建/修改：
- src/systems/ScaleSystem.ts
- src/systems/FactorySystem.ts
- src/ui/HUD.ts
- 整合到 MainScene.ts

到此核心循环就完整了：喂养 → 满意 → 龙鳞 → 工厂 → 更多食物 → 喂养
```

### Step 5 Prompt：情感打磨

```markdown
核心循环完成。现在我要让龙宝宝"活起来"——让玩家产生情感连接。

需求：
1. 表情气泡系统：
   - 龙宝宝头顶偶尔出现文字气泡（持续 2 秒后淡出）
   - 触发条件：
     - 饥饿度 < 30：随机说 "好饿..." / "肚子叫了..." / "想吃东西..."
     - 吃完食物：随机说 "好吃！" / "还要！" / "啊呜~" / "幸福..."
     - 满意度 > 80：偶尔说 "好开心！" / "最喜欢主人了" / "今天真好~"
     - 满意度 < 30：偶尔说 "..." / "不开心" / "..."
     - 喂偏爱食物：必定说 "这是我最爱的！"
   - 对话池放在 src/data/DialogueData.ts，方便后续扩展

2. 偏好系统：
   - 游戏开始时随机选择一种食物作为"最爱"
   - 屏幕某处显示提示："我家龙宝宝似乎特别喜欢 ?"（小字）
   - 喂最爱时：满意度加成 ×2，必定触发偏爱台词

3. 给龙宝宝加 1-2 个细节动画：
   - 吃东西时身体轻微震动
   - 满意度高时偶尔蹦跳一下
   - 饥饿时身体轻微下垂

4. 启动画面（简单）：
   - 游戏开始时显示："你发现了一颗龙蛋..."（3 秒淡入）
   - 然后："决定收养它，给它取名为：[输入名字]"
   - 玩家输入名字后进入游戏，龙宝宝头上显示名字

请创建/修改：
- src/data/DialogueData.ts
- src/entities/SpeechBubble.ts
- src/systems/PreferenceSystem.ts
- src/scenes/StartScene.ts
- 整合到现有代码

让玩家爱上自己的龙宝宝。
```

---

## ? 使用建议

### 怎么用这些 Prompt？

#### 推荐方式 1：先用版本 A 走完一遍

```
1. 复制版本 A 的完整 Prompt 到 Cursor
2. AI 会带你走完 Step 1
3. 你确认后 AI 自动进入 Step 2
4. 5 个 Step 走完，原型就完成了
```

**预计时间**：3-5 个小时（取决于你边做边改的程度）

#### 推荐方式 2：分阶段用版本 B

```
1. 用 Step 1 Prompt 搭好骨架
2. 跑通后再用 Step 2 Prompt
3. 每步独立完成，更可控
```

**适合**：你想边做边深入理解每个部分

### 最重要的几个 Tips

#### ? Tip 1：保持 vibe，但守住边界

```
当 AI 想给你加"科技树系统"、"存档功能"、"成就系统" 时——
拒绝。

你的目标是：3-5 小时内看到屏幕上有一只可爱的、会饿的龙。
其他都是干扰。
```

#### ? Tip 2：先丑后美

```
不要纠结：
- 龙宝宝的颜色搭配
- UI 的字体
- 按钮的圆角

先让它能跑。
能跑了再美化。
```

#### ? Tip 3：随时和 AI 对话

```
当你觉得某段代码有问题：
"这段代码我不太理解，能用 UE 蓝图的概念解释吗？"

当你想改设计：
"我觉得 3 秒掉 1 点饥饿太快了，改成 5 秒。"

当你卡住：
"npm run dev 报错 XXX，怎么解决？"
```

#### ? Tip 4：让 AI 同步给你解释

```
在 Prompt 中加这句：
"每完成一个文件，简短解释这个文件做什么，
特别是 UE 里没有对应概念的部分。"

这样你边做边学，不会变成"复制粘贴的码农"。
```

---

## ? 常见的"坑"提前预警

基于 UE 开发者切换到 Phaser 的常见问题：

### 坑 1：模块导入

```typescript
// UE 没有这种东西。Phaser 项目里你会经常看到：
import Phaser from 'phaser';
import { Dragon } from './entities/Dragon';

// 等同于 UE 里 #include
// 但路径用 '/' 不是 '\\'
// 而且不需要 .ts 扩展名
```

### 坑 2：异步加载

```typescript
// Phaser 的资源加载是异步的
// preload() 加载所有资源
// create() 资源加载完后调用
// update(time, delta) 每帧调用（类似 Tick）
```

### 坑 3：Scene 不是 Level

```
UE Level 切换会卸载所有内容
Phaser Scene 切换可以保留状态（共存）
你可以同时有多个活跃 Scene（如 GameScene + HUDScene）
```

### 坑 4：没有 Blueprint

```
你的所有逻辑都在代码里。
没有可视化连接。
但好处是：AI 能直接写代码
不像 UE 蓝图，AI 帮不上忙
```

---

## ? 你的下一步

### 现在马上做的事

```
1. 打开终端，进到你的工作目录
2. 打开 Cursor
3. 把版本 A 的 Prompt 复制进去
4. 按 Enter
5. 跟着 AI 一步步走
```

**预计 30 分钟内你就能看到屏幕上第一个画面**。
**预计 3-5 小时你就能看到屏幕上第一只会饿的龙**。

### 完成原型后

```
1. 录一段 30 秒视频
2. 发给 5 个朋友
3. 不要解释玩法，让他们自己摸索
4. 观察他们的反应：
   ? "好可爱！" → 你成功了
   ? 自发给龙取名字 → 你成功了
   ? 玩了 10 分钟还在玩 → 你大成功
   ?? 表情困惑 → 调整 UI
   ? 立刻关掉 → 重新思考核心
```

---

## ? 最后一句话

```
不要追求完美。
追求"今天能不能跑起来"。

不要追求"完整功能"。
追求"核心循环爽不爽"。

不要担心"代码不够优雅"。
原型期的代码本来就是一次性的。

你的目标只有一个：
让那只龙宝宝出现在屏幕上，
让你（不是别人，是你自己）想再多喂它一口。

如果你做到了——这个游戏就有救了。
如果你做不到——再多机制也救不了它。
```

---

去吧。复制 Prompt，打开 Cursor，让那只小龙诞生。??

期待 3 小时后听到你说："它会饿了！它在看我！"